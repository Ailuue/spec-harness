# src/ — intentionally empty

This is where an implementation would live if the harness had an `/implement`
phase. It doesn't. This lab is about the upstream discipline (specify → plan →
tasks → analyze), not code generation. Leaving this empty is the point: the
deliverable is [`../runs/LOG.md`](../runs/LOG.md), not a working app.
