{
  "name": "spec-harness-tools",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Harness tooling: Zod schemas for spec/plan/tasks artifacts + a deterministic FR->plan->task traceability checker. NOT the expense tracker (src/ stays empty); this enforces the upstream discipline mechanically instead of by prose.",
  "bin": {
    "trace": "src/trace.ts"
  },
  "scripts": {
    "trace": "tsx src/trace.ts",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "mdast-util-to-string": "^4.0.0",
    "remark-gfm": "^4.0.0",
    "remark-parse": "^11.0.0",
    "unified": "^11.0.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/mdast": "^4.0.4",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
